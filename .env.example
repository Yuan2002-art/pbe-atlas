# The MVP needs no environment variables — it reads Markdown files from data/.
#
# Reserved for later phases:
# ANTHROPIC_API_KEY=      # future AI-assisted case research (src/app/api/)
# DATABASE_URL=           # future database, if you outgrow Markdown files
